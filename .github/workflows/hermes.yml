name: Hermes Streaming Engine

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  stream:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install gdown & FFmpeg
        run: |
          python3 -m pip install --upgrade gdown
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Download Video from Google Drive
        run: |
          # Kita hapus dulu lofi.mp4 bawaan repo biar gak error 'File already exists'
          rm -f lofi.mp4
          # Pakai ID video kamu yang tadi
          python3 -m gdown 14BscG_Lp_qQkVqJNfFN5W9wqCO_PRRxj -O lofi.mp4

      - name: Start Looping Stream
        run: |
          ffmpeg -re -stream_loop -1 -i lofi.mp4 \
          -c:v libx264 -preset veryfast -b:v 2500k -maxrate 2500k -bufsize 5000k \
          -pix_fmt yuv420p -g 120 -c:a aac -b:a 128k -ar 44100 \
          -f flv "rtmp://a.rtmp.youtube.com/live2/${{ secrets.YOUTUBE_STREAM_KEY }}"
