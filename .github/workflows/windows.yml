name: RDP1

on:
  workflow_dispatch:

jobs:
  rdp-job:
    runs-on: windows-2022
    timeout-minutes: 360

    steps:

      # =========================
      # BASIC SETUP
      # =========================

      - name: Enable Script Execution
        run: Set-ExecutionPolicy Bypass -Scope Process -Force

      - name: Enable RDP
        run: |
          Set-ItemProperty `
            -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
            -Name "fDenyTSConnections" `
            -Value 0

          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Restart-Service TermService -Force


      - name: Create RDP User
        run: |
          $password = ConvertTo-SecureString "@Whatsapp2" -AsPlainText -Force

          New-LocalUser -Name "dolvin" -Password $password -AccountNeverExpires -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Administrators" -Member "dolvin"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "dolvin"

          echo "RDP_USER=dolvin" >> $env:GITHUB_ENV
          echo "RDP_PASS=@Whatsapp2" >> $env:GITHUB_ENV


      # =========================
      # SUPER+++ LIGHT OPTIMIZATION
      # =========================

      - name: SUPER OPTIMIZATION
        run: |

          powercfg -setactive SCHEME_MIN
          powercfg -hibernate off
          powercfg -change -monitor-timeout-ac 0
          powercfg -change -standby-timeout-ac 0

          # Disable Defender realtime (tanpa matiin servicenya total)
          Set-MpPreference -DisableRealtimeMonitoring $true -ErrorAction SilentlyContinue

          # Disable telemetry ringan
          Stop-Service DiagTrack -Force -ErrorAction SilentlyContinue
          Set-Service DiagTrack -StartupType Disabled -ErrorAction SilentlyContinue

          # Disable service yang gak penting
          $services = @("SysMain","WSearch","MapsBroker","Fax","Spooler","RemoteRegistry","WerSvc")
          foreach ($svc in $services) {
            Stop-Service $svc -Force -ErrorAction SilentlyContinue
            Set-Service $svc -StartupType Disabled -ErrorAction SilentlyContinue
          }

          # Visual effect off
          reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects" /v VisualFXSetting /t REG_DWORD /d 2 /f
          reg add "HKCU\Control Panel\Desktop" /v MenuShowDelay /t REG_SZ /d 0 /f
          reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize" /v EnableTransparency /t REG_DWORD /d 0 /f

          fsutil behavior set disablelastaccess 1

          Write-Host "SUPER MODE ACTIVE"


      # =========================
      # TAILSCALE
      # =========================

      - name: Install Tailscale
        run: |
          Invoke-WebRequest `
            https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi `
            -OutFile "$env:TEMP\tailscale.msi"

          Start-Process msiexec.exe `
            -ArgumentList "/i $env:TEMP\tailscale.msi /quiet /norestart" `
            -Wait

      - name: Start Tailscale
        run: Start-Service Tailscale

      - name: Connect Tailscale
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up `
            --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} `
            --hostname=gh-runner-${{ github.run_id }} `
            --accept-routes `
            --accept-dns=false `
            --reset

          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "TS_IP=$ip" >> $env:GITHUB_ENV


      # =========================
      # DEPENDENCY
      # =========================

      - name: Install VC++
        run: |
          Invoke-WebRequest `
            https://aka.ms/vs/17/release/vc_redist.x64.exe `
            -OutFile "$env:TEMP\vc.exe"

          Start-Process "$env:TEMP\vc.exe" `
            -ArgumentList "/install /quiet /norestart" `
            -Wait


      - name: Install gdown
        run: python -m pip install gdown


      # =========================
      # DOWNLOAD & INSTALL LAUNCHER
      # =========================

      - name: Download bahn.zip
        run: |
          $output = "$env:TEMP\bahn.zip"
          python -m gdown 1YLPDhH0-fgaXihTshYgDCYFQ0c1CFbZp -O $output
          echo "BAHN_ZIP=$output" >> $env:GITHUB_ENV

      - name: Extract bahn
        run: |
          Expand-Archive -Path $env:BAHN_ZIP -DestinationPath "C:\" -Force

      - name: Install Launcher
        run: |
          $installer = "C:\bahn\bahn\Install_POINTBLANK_20251111.exe"
          if (Test-Path $installer) {
            Start-Process $installer -ArgumentList "/S" -Wait
          }


      # =========================
      # DOWNLOAD GAME DATA (POINTBLANK ID)
      # =========================

      - name: Download PointBlank ID.zip
        run: |
          $output = "$env:TEMP\PB_GAME.zip"
          python -m gdown 1mLMBZfpndqcdC_CtKkonwHlFD6gJlNM0 -O $output
          echo "PB_ZIP=$output" >> $env:GITHUB_ENV

      - name: Extract Game Data
        run: |
          Expand-Archive -Path $env:PB_ZIP -DestinationPath "C:\Zepetto" -Force
          Remove-Item $env:PB_ZIP -Force


      # =========================
      # OPEN LAUNCHER
      # =========================

      - name: Open Launcher
        run: Start-Process "C:\Zepetto\Z-Launcher(ID)\ZLauncher.exe"


      - name: Show Info
        run: |
          Write-Host ""
          Write-Host "=============================="
          Write-Host "RDP READY"
          Write-Host "IP   : $env:TS_IP"
          Write-Host "USER : $env:RDP_USER"
          Write-Host "PASS : $env:RDP_PASS"
          Write-Host "=============================="
          Write-Host ""


      - name: Keep Alive
        run: |
          while ($true) {
            Write-Host "RDP Active..."
            Start-Sleep -Seconds 300
          }
